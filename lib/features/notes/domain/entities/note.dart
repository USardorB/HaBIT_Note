class Note {
  final int id;
  final int colorId;
  final String title;
  final String description;

  const Note({
    required this.id,
    required this.colorId,
    required this.title,
    required this.description,
  });
}
